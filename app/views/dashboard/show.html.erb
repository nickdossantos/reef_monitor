<%tab "dashboard"%>

<h4><%=@tank.name%></h4>

<%if @user.has_api_configured? && @tank.has_temp_sensor_configured?%>
    <%= render 'temperature_widget'%>
<%end%>

<%= render 'sensor_section'%>
<%= render 'reading_section'%>
<%= render 'device_section'%>
<%= render 'tank_sensors_section'%>

<%= render 'device_modal'%>
<%= render 'sensor_modal'%>
<%= render 'reading_modal'%>
